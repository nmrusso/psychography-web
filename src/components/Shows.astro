<section class="shows" id="shows" data-screen-label="07 Shows">
  <div class="wrap">
    <header class="sec-head reveal">
      <div>
        <div class="sec-head__kicker">
          <span class="hairline"></span>
          <span class="sec-head__num" data-i18n="shows.kicker">07 — Upcoming Shows</span>
        </div>
      </div>
      <h2 class="sec-head__title" data-i18n-html="shows.title">Coming soon,<br><em>live</em>.</h2>
    </header>

    <div class="shows__empty reveal">
      <div class="shows__empty-mark">
        <span class="hairline"></span>
        <span class="shows__empty-kicker" data-i18n="shows.empty.kicker">— No dates announced</span>
        <span class="hairline"></span>
      </div>
      <p class="shows__empty-title" data-i18n-html="shows.empty.title">We're preparing<br>our <em>first shows</em>.</p>
      <p class="shows__empty-desc" data-i18n="shows.empty.desc">
        We don't have confirmed dates yet. Sign up by email or follow us on social media to be among the first to know when we announce our first live show.
      </p>
      <a href="#contact" class="shows__empty-cta">
        <span data-i18n="shows.empty.cta">Let me know when there's a date</span> <span class="arrow"></span>
      </a>
    </div>
  </div>
</section>
